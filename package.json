{
  "name": "geun",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "python3 -m http.server 0 -d public",
    "predeploy": "rm -rf dist && cp -R public dist && COMMIT=$(git rev-parse --short HEAD) && sed \"s/__COMMIT__/${COMMIT}/g\" dist/index.html > dist/index.tmp && mv dist/index.tmp dist/index.html",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^3.0.0",
    "gh-pages": "^3.2.3"
  }
}